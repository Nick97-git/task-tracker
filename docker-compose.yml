version: "3"
services:
  mysql:
    image: mysql
    container_name: mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=
      - MYSQL_PASSWORD=root
      - MYSQL_USER=nick
      - MYSQL_DATABASE=task_tracker
  app:
    container_name: spring-boot-project
    image: spring-boot-project
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/task_tracker?serverTimezone=UTC
    depends_on:
      - mysql